[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "obsidian-bookreading"
version = "0.1.0"
description = "AI tools for the Obsidian Bookreading knowledge base"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
